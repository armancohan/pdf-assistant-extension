{
  "name": "arxiv-mm-summarizer",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension that summarizes arxiv papers using LLM APIs",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {}
}
